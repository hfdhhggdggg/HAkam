# وثائق نظام إدارة حكام الاتحاد

## نظرة عامة

نظام لإدارة حكام الاتحاد الرياضي مبني على قالب Mosaic React. يحتوي على أربع صفحات رئيسية: تسجيل الدخول، انتظار المراجعة، لوحة تحكم الإدارة، وبروفايل الحكم. النظام يدعم الوضع الداكن، التصميم المتجاوب، ويعمل باللغة العربية.

---

## بيانات تجريبية (Mock Data)

بما أن قاعدة البيانات غير متاحة حالياً، النظام يعمل ببيانات تجريبية مخزّنة في `src/lib/mockData.js`. كل البيانات مهيأة لتُستبدل لاحقاً بقاعدة بيانات Supabase بدون تغيير الواجهة.

### الحسابات التجريبية

| الدور | اسم المستخدم | كلمة المرور | الصفحة بعد الدخول |
|------|------------|-----------|-----------------|
| إدارة الاتحاد | `admin` | `admin123` | لوحة التحكم |
| حكم | الرقم القومي من القائمة | `referee123` | بروفايل الحكم |
| حكم (بانتظار المراجعة) | رقم قومي لحكم بحالة `pending` | `referee123` | صفحة انتظار المراجعة |

### البيانات المتوفرة

- **8 حكام** بأسماء وأرقام قومية ورتب ومحافظات وحالات مختلفة
- **9 اختبارات لياقة** بنتائج وأزمنة وأسباب فشل
- **3 طلبات تسجيل جديدة** في انتظار المراجعة
- **5 نشاطات** (تسجيل نتائج، طلبات تسجيل، تفعيل/تعطيل حسابات)
- إحصائيات: عدد الحكام حسب الرتبة، التوزيع الجغرافي، نسب النجاح، أسباب الرسوب، تطور النتائج عبر المواسم

---

## الصفحات

### 1. صفحة تسجيل الدخول (`/signin`)

**الملف:** `src/pages/SignIn.jsx`

صفحة مستقلة تماماً بدون شريط جانبي أو رأس. خلفية رمادية فاتحة (رمادية داكنة في الوضع الليلي)، فيها كارت واحد في منتصف الشاشة.

**المكونات:**

- **مبدّل الوضع الداكن** — في الزاوية العلوية اليسرى، نفس مكون ThemeToggle المستخدم في باقي الموقع
- **الشعار** — الشعار البنفسجي الدائري الأصلي لـ Mosaic في أعلى الكارت
- **العنوان** — "تسجيل الدخول" مع نص فرعي "اتحاد الحكام الرياضيين"
- **حقل الرقم القومي** — نص عادي، بنفس ستايل حقول الإدخال في الموقع (form-input)
- **حقل كلمة المرور** — حقل مخفي النص، نفس الستايل
- **رسالة الخطأ** — تظهر في صندوق أحمر خفيف إذا فشل الدخول (رقم قومي/كلمة مرور غير صحيحة)
- **زر "تسجيل الدخول"** — زر أساسي بلون داكن (فاتح في الوضع الليلي):
  - الحالة العادية: نص "تسجيل الدخول"
  - حالة التحميل: spinner دوّار + نص "جارٍ التحقق…" + الزر معطّل (شفافية مخفّضة + مؤشر not-allowed)
  - التحميل يستغرق 1.2 ثانية (محاكاة طلب شبكة)
- **نص تلميحي** — في أسفل الكارت يوضح بيانات الدخول التجريبية

**المنطق:**

- عند نجاح الدخول كإدارة: توجيه إلى `/dashboard`
- عند نجاح الدخول كحكم نشط: توجيه إلى `/profile`
- عند نجاح الدخول كحكم بحالة `pending`: توجيه إلى `/pending`
- الجلسة تُحفظ في `sessionStorage` (تُفقد عند إغلاق المتصفح)

---

### 2. صفحة انتظار المراجعة (`/pending`)

**الملف:** `src/pages/PendingReview.jsx`

صفحة مستقلة بنفس بنية صفحة الدخول (كارت مفرد بدون شريط جانبي).

**المكونات:**

- **مبدّل الوضع الداكن** — في الزاوية العلوية اليسرى
- **أيقونة الحالة** — دائرة برتقالية فاتحة فيها علامة تعجّب برتقالية
- **نص الحالة** — "في انتظار المراجعة" مع رسالة ترحيب باسم الحكم
- **الرقم المرجعي** — في صندوق رمادي، بصيغة `REF-000001` (مأخوذ من معرّف الحكم)
- **رابط "تواصل مع الإدارة"** — رابط بنفسجي يعيد إلى صفحة الدخول حالياً (جاهز لربطه بصفحة تواصل فعلية لاحقاً)

**ملاحظة:** لا يوجد أي عنصر تفاعلي آخر — لا أزرار، لا حقول، لا تنقل. الصفحة إخبارية بحتة.

---

### 3. لوحة تحكم الإدارة (`/dashboard`)

**الملف:** `src/pages/Dashboard.jsx`

محمية بصلاحية الإدارة فقط (إذا دخل حكم يحوّل تلقائياً إلى `/profile`).

تستخدم بنية Dashboard الأصلية بالكامل: شريط جانبي + رأس + شبكة بطاقات 12 عمود. تم حذف البانر الترويجي لـ Cruip.

#### الشريط الجانبي

**الملف:** `src/partials/Sidebar.jsx`

معاد بناؤه بالكامل. المجموعات والروابط:

| المجموعة | الروابط | الحالة |
|---------|--------|-------|
| **الصفحات** | | |
| | الرئيسية ← نظرة عامة | يعمل (الصفحة الحالية) |
| | الرئيسية ← إحصائيات | رابط موجود (يحتاج صفحة) |
| | الحكام ← قائمة الحكام - Tabs | رابط موجود (يحتاج صفحة) |
| | الحكام ← قائمة الحكام - Tiles | رابط موجود (يحتاج صفحة) |
| | الحكام ← بروفايل حكم | يعمل (يحوّل إلى `/profile`) |
| | اختبارات اللياقة ← Kanban | رابط موجود (يحتاج صفحة) |
| | اختبارات اللياقة ← List | رابط موجود (يحتاج صفحة) |
| | الرسائل | رابط موجود (يحتاج صفحة) — عليه شارة "4" |
| | طلبات التسجيل | رابط موجود (يحتاج صفحة) |
| | التقويم | رابط موجود (يحتاج صفحة) |
| **المزيد** | | |
| | المصادقة ← تسجيل الدخول | يعمل (يحوّل إلى `/signin`) |

**تم حذف هذه المجموعات بالكامل:** E-Commerce، Finance، Job Board، Campaigns، Sign up (من قسم المصادقة).

**ميزات الشريط الجانبي:**
- يتطوى/يتوسع على الشاشات الكبيرة (زر في الأسفل)
- يظهر كقائمة منسدلة على الجوال (زر همبرغر في الرأس)
- الروابط النشطة تظهر باللون البنفسجي
- الشعار البنفسجي في الأعلى

#### الرأس (Header)

**الملف:** `src/partials/Header.jsx`

نفس بنية Header الأصلية بدون تعديل. يحتوي على:

- **زر البحث** — يفتح نافذة بحث منبثقة (ModalSearch)
- **الإشعارات** — قائمة منسدلة (DropdownNotifications) — مخصّصة لطلبات التسجيل الجديدة
- **المساعدة** — قائمة منسدلة (DropdownHelp)
- **مبدّل الوضع الداكن** (ThemeToggle)
- **قائمة البروفايل** (DropdownProfile) — معدّلة:

**التعديلات على قائمة البروفايل** (`src/components/DropdownProfile.jsx`):
- الاسم يظهر من بيانات المستخدم المسجّل (بدل "Acme Inc.")
- الدور يظهر كـ "معد بدني" للإدارة أو "حكم" للحكم (بدل "Administrator")
- رابط "البروفايل" يحوّل إلى `/profile`
- زر "تسجيل الخروج" يمسح الجلسة ويحوّل إلى `/signin`

#### البطاقات (01-13)

كل بطاقة في `src/partials/dashboard/DashboardCardXX.jsx`:

##### البطاقة 01 — حكام دوليون
- **النوع:** LineChart (رسم خطي)
- **المحتوى:** عدد الحكام الدوليين عبر 5 مواسم (1→2)
- **العناصر:** عنوان، قائمة تحرير منسدلة (عرض التفاصيل/تصدير)، الرقم الحالي (2)، شارة "+1" خضراء
- **الرسم:** خط بنفسجي مع تدرّج + خط رمادي للمقارنة

##### البطاقة 02 — حكام الرتبة الأولى
- **النوع:** LineChart
- **المحتوى:** عدد حكام الرتبة الأولى عبر 5 مواسم (2→3)
- **نفس بنية البطاقة 01 تماماً**

##### البطاقة 03 — حكام الرتبة الثانية
- **النوع:** LineChart
- **المحتوى:** عدد حكام الرتبة الثانية عبر 5 مواسم (2→3)
- **نفس بنية البطاقة 01 تماماً**

##### البطاقة 04 — ناجح VS راسب
- **النوع:** BarChart (رسم أعمدة)
- **المحتوى:** مقارنة الناجحين والراسبين في اختبار اللياقة عبر 3 مواسم
- **الألوان:** أخضر للناجح، أحمر للراسب
- **الأعمدة:** موسم 22-23 (42 ناجح/8 راسب)، 23-24 (48/6)، 24-25 (38/12)

##### البطاقة 05 — الاختبارات الجارية الآن
- **النوع:** عداد مباشر (بدون رسم بياني)
- **المحتوى:** رقم كبير يتغيّر ديناميكياً كل 3 ثوانٍ (بين 0 و20)
- **العناصر:** نقطة خضراء نابضة (animate-ping) بجانب العنوان، الرقم، نص "اختبار لياقة قيد التنفيذ"، شارة "تحديث مباشر" خضراء
- **المنطق:** useState + useEffect مع setInterval، يزيد/ينقص عشوائياً

##### البطاقة 06 — توزيع الحكام حسب المحافظة
- **النوع:** DoughnutChart (رسم دائري)
- **المحتوى:** توزيع 8 حكام على 7 محافظات
- **الألوان:** بنفسجي، سماوي، بنفسجي داكن، أخضر، كهرماني، أحمر، رمادي
- **المحافظات:** القاهرة (2)، الإسكندرية (1)، الجيزة (1)، المنوفية (1)، الغربية (1)، أسيوط (1)، الشرقية (1)

##### البطاقة 07 — نسب النجاح في كل نوع اختبار
- **النوع:** جدول مع أشرطة تقدّم
- **المحتوى:** نوعان من الاختبارات:
  - RSA (Interval): 45 إجمالي، 38 ناجح، 7 راسب، 84.4% نسبة نجاح
  - تحمل: 30 إجمالي، 22 ناجح، 8 راسب، 73.3% نسبة نجاح
- **العناصر:** أيقونة دائرية ملوّنة لكل نوع، أرقام الناجح (أخضر) والراسب (أحمر)، شريط تقدّم ملوّن + نسبة مئوية

##### البطاقة 08 — تطور نتائج اللياقة عبر المواسم
- **النوع:** LineChart02 (رسم خطي مع تدرّج)
- **المحتوى:** مقارنة نتائج الموسم الحالي مع الموسم السابق عبر 5 مواسم
- **الخطين:** بنفسجي للنتائج الحالية، سماوي للموسم السابق
- **العناصر:** قائمة اختيار تاريخ (DateSelect) في الرأس

##### البطاقة 09 — الشوطين الفاشلة حسب السبب
- **النوع:** BarChart02 (رسم أعمدة)
- **المحتوى:** عدد الاختبارات الفاشلة مصنّفة حسب سبب الفشل لكل موسم
- **الألوان:** أحمر لتأخر الجري، كهرماني لتأخر المشي
- **العناصر:** تلميح (Tooltip) بجانب العنوان، الرقم الإجمالي (17)، شارة "+42%" حمراء

##### البطاقة 10 — جدول الحكام
- **النوع:** جدول
- **المحتوى:** 5 حكام بصور وأسماء ورتب وآخر اختبار وحالة
- **الأعمدة:** الاسم (صورة + اسم)، الرتبة، آخر اختبار، الحالة (نشط=أخضر/معطّل=أحمر/بانتظار=برتقالي)، قائمة تحرير (عرض/تعديل)
- **الصور:** من صور الموقع الأصلية (user-36-05 إلى 09)

##### البطاقة 11 — أسباب الرسوب
- **النوع:** BarChart03 (رسم أعمدة أفقية)
- **المحتوى:** 4 أسباب للرسوب:
  - تأخر جري: 7 (أحمر)
  - تأخر مشي: 4 (كهرماني)
  - عدم استكمال الاختبار: 3 (بنفسجي)
  - إصابة أثناء الاختبار: 2 (سماوي)
- **العناصر:** الرقم الإجمالي (16)، شارة "+42%" حمراء

##### البطاقة 12 — آخر النشاطات
- **النوع:** قائمة نشاطات
- **المحتوى:** مقسّمة لمجموعتين:
  - **اليوم:** تسجيل نتيجة (أخضر)، طلب تسجيل جديد (بنفسجي)، تفعيل حساب (سماوي)
  - **أمس:** تسجيل نتيجة (أخضر)، تعطيل حساب (أحمر)
- **العناصر:** أيقونة دائرية ملوّنة لكل نشاط، نص الوصف، رابط "عرض ←" لكل نشاط، رابط "عرض الكل ←" في الأسفل

##### البطاقة 13 — تطور عدد الاختبارات عبر المواسم
- **النوع:** LineChart02 (رسم خطي مع تدرّج)
- **المحتوى:** عدد الاختبارات عبر 5 مواسم (60→50)
- **العناصر:** قائمة اختيار تاريخ، الرقم الحالي (50)، شارة "-7.4%" حمراء

**ملاحظة:** تم حذف البطاقة المالية الأصلية (Income/Expenses) بالكامل.

---

### 4. صفحة بروفايل الحكم (`/profile`)

**الملف:** `src/pages/RefereeProfile.jsx`

محمية (تتطلب تسجيل دخول). تستخدم بنية Profile من قسم Community الأصلي.

**البنية:**

- **شريط جانبي + رأس** — نفس مكونات لوحة التحكم
- **عنوان الصفحة** — "بروفايل الحكم"

**بطاقة البروفايل:**
- **غلاف علوي** — شريط ملوّن بتدرّج من البنفسجي إلى السماوي
- **صورة الحكم** — دائرية بحافة بيضاء/داكنة، بارزة فوق الغلاف
- **الاسم** — كبير وبارز
- **معلومات سريعة** — الرتبة، المحافظة، الحالة (بنفس ألوان الحالة: أخضر/أحمر/برتقالي)
- **زر "تعديل البيانات"** — زر أساسي (يحتاج ربط بصفحة تعديل)

**شبكة معلومات (3 بطاقات):**
- الرقم القومي
- الهاتف
- تاريخ التسجيل

**التبويبات:**

**تبويب "اختبارات اللياقة":**
- جدول بكل اختبارات الحكم
- الأعمدة: نوع الاختبار، الموسم، زمن الجري، زمن المشي، النتيجة (ناجح=أخضر/راسب=أحمر)، سبب الفشل، التاريخ
- رسالة "لا توجد اختبارات مسجلة" إذا لم يكن للحكم اختبارات

**تبويب "البيانات الشخصية":**
- شبكة بمعلومتين لكل صف: الاسم الكامل، الرقم القومي، الرتبة، المحافظة، الحالة، الهاتف

---

## المصادقة والتوجيه

### ملف المصادقة

**الملف:** `src/utils/AuthContext.jsx`

- `AuthProvider` يلفّ التطبيق بالكامل
- `useAuth()` hook يوفّر: `user`، `role`، `signIn()`، `signOut()`، `loading`
- الجلسة تُحفظ في `sessionStorage` (مفتاحان: `auth_user` و `auth_role`)
- `signIn(nationalId, password)` يعيد Promise — يحل بنجاح أو يرفض مع رسالة خطأ
- التأخير 1.2 ثانية لمحاكاة طلب شبكة

### التوجيه (Routing)

**الملف:** `src/App.jsx`

| المسار | الصفحة | الحماية |
|-------|-------|--------|
| `/` | توجيه تلقائي إلى `/signin` | — |
| `/signin` | صفحة تسجيل الدخول | عام |
| `/pending` | صفحة انتظار المراجعة | عام |
| `/dashboard` | لوحة تحكم الإدارة | يتطلب صلاحية `admin` |
| `/profile` | بروفايل الحكم | يتطلب تسجيل دخول |

**`ProtectedRoute`:**
- إذا لم يكن هناك مستخدم: يحوّل إلى `/signin`
- إذا طُلبت صلاحية `admin` والمستخدم ليس إدارة: يحوّل إلى `/profile`

### نقطة الدخول

**الملف:** `src/main.jsx`

ترتيب المزوّدات (Providers):
1. `BrowserRouter` (التوجيه)
2. `ThemeProvider` (الوضع الداكن)
3. `AuthProvider` (المصادقة)
4. `App`

---

## نظام الألوان

| اللون | المعنى | الاستخدام |
|------|-------|----------|
| أخضر (`green-500`) | نجاح / نشط | نتائج النجاح، الحالة النشطة، شارات الزيادة الإيجابية |
| أحمر (`red-500`) | رسوب / معطّل / سلبي | نتائج الرسوب، الحالة المعطّلة، شارات الانخفاض |
| برتقالي (`amber-500`) | تحذير / انتظار | حالة "بانتظار المراجعة"، أسباب الفشل |
| بنفسجي (`violet-500`) | أساسي / روابط نشطة | الشعار، الروابط النشطة، الأزرار الأساسية |
| سماوي (`sky-500`) | ثانوي | خطوط المقارنة، تفعيل الحساب |
| كهرماني (`amber-500`) | تنبيه | تأخر المشي |

---

## الصفحات والأزرار غير الشغّالة حالياً

هذه الروابط موجودة في الواجهة لكنها لا تؤدي إلى صفحات فعلية بعد (تؤدي إلى `#0` أو مسارات غير معرّفة):

### من الشريط الجانبي

| الرابط | المسار | الحالة |
|-------|-------|-------|
| الرئيسية ← إحصائيات | `/dashboard/stats` | مسار غير معرّف — يحتاج صفحة إحصائيات منفصلة |
| الحكام ← قائمة الحكام - Tabs | `/referees/tabs` | مسار غير معرّف — يحتاج صفحة قائمة بالتبويبات |
| الحكام ← قائمة الحكام - Tiles | `/referees/tiles` | مسار غير معرّف — يحتاج صفحة قائمة بالبطاقات |
| اختبارات اللياقة ← Kanban | `/tasks/kanban` | مسار غير معرّف — يحتاج لوحة Kanban لمتابعة الاختبارات |
| اختبارات اللياقة ← List | `/tasks/list` | مسار غير معرّف — يحتاج قائمة بكل نتائج الاختبارات |
| الرسائل | `/messages` | مسار غير معرّف — يحتاج صفحة رسائل بين الإدارة والحكام |
| طلبات التسجيل | `/inbox` | مسار غير معرّف — يحتاج صفحة Inbox لطلبات التسجيل الجديدة |
| التقويم | `/calendar` | مسار غير معرّف — يحتاج تقويم لمواعيد الاختبارات والمباريات |

### من البطاقات

| البطاقة | العنصر | الحالة |
|-------|-------|-------|
| 01-03 | قائمة "عرض التفاصيل" / "تصدير" | روابط على `#0` — تحتاج وجهات فعلية |
| 10 | قائمة "عرض" / "تعديل" لكل حكم | روابط على `#0` — تحتاج صفحة تفاصيل/تعديل حكم |
| 12 | رابط "عرض ←" لكل نشاط | روابط على `#0` — تحتاج صفحة تفاصيل نشاط |
| 12 | رابط "عرض الكل ←" | رابط على `#0` — يحتاج صفحة سجل النشاطات الكامل |

### من بروفايل الحكم

| العنصر | الحالة |
|-------|-------|
| زر "تعديل البيانات" | لا يؤدي إلى شيء — يحتاج صفحة/نموذج تعديل |

### من صفحة انتظار المراجعة

| العنصر | الحالة |
|-------|-------|
| رابط "تواصل مع الإدارة" | يحوّل إلى `/signin` حالياً — يحتاج صفحة تواصل فعلية أو بريد إدارة |

### من الرأس (Header)

| العنصر | الحالة |
|-------|-------|
| زر البحث | يفتح نافذة بحث منبثقة — البحث نفسه تجريبي (لا يبحث في بيانات فعلية) |
| الإشعارات | قائمة منسدلة بمحتوى تجريبي — تحتاج ربط بطلبات التسجيل الفعلية |
| المساعدة | قائمة منسدلة بمحتوى تجريبي |

---

## الملفات المعدّلة

| الملف | التغيير |
|------|--------|
| `src/App.jsx` | إعادة كتابة كاملة — توجيه + حماية المسارات |
| `src/main.jsx` | إضافة AuthProvider |
| `src/utils/AuthContext.jsx` | إعادة كتابة كاملة — سياق المصادقة |
| `src/partials/Sidebar.jsx` | إعادة كتابة كاملة — مجموعات وروابط جديدة |
| `src/pages/Dashboard.jsx` | حذف البانر، تغيير العنوان، حذف استيراد Banner |
| `src/pages/SignIn.jsx` | إعادة كتابة كاملة |
| `src/pages/PendingReview.jsx` | ملف جديد |
| `src/pages/RefereeProfile.jsx` | ملف جديد |
| `src/lib/mockData.js` | ملف جديد — كل البيانات التجريبية |
| `src/components/DropdownProfile.jsx` | تعديل — اسم/دور المستخدم + زر خروج |
| `src/partials/dashboard/DashboardCard01.jsx` | إعادة كتابة — حكام دوليون |
| `src/partials/dashboard/DashboardCard02.jsx` | إعادة كتابة — حكام رتبة أولى |
| `src/partials/dashboard/DashboardCard03.jsx` | إعادة كتابة — حكام رتبة ثانية |
| `src/partials/dashboard/DashboardCard04.jsx` | إعادة كتابة — ناجح VS راسب |
| `src/partials/dashboard/DashboardCard05.jsx` | إعادة كتابة — عداد مباشر |
| `src/partials/dashboard/DashboardCard06.jsx` | إعادة كتابة — توزيع المحافظات |
| `src/partials/dashboard/DashboardCard07.jsx` | إعادة كتابة — نسب نجاح أنواع الاختبارات |
| `src/partials/dashboard/DashboardCard08.jsx` | إعادة كتابة — تطور نتائج اللياقة |
| `src/partials/dashboard/DashboardCard09.jsx` | إعادة كتابة — الشوطين الفاشلة |
| `src/partials/dashboard/DashboardCard10.jsx` | إعادة كتابة — جدول الحكام |
| `src/partials/dashboard/DashboardCard11.jsx` | إعادة كتابة — أسباب الرسوب |
| `src/partials/dashboard/DashboardCard12.jsx` | إعادة كتابة — آخر النشاطات |
| `src/partials/dashboard/DashboardCard13.jsx` | إعادة كتابة — تطور عدد الاختبارات |

---

## الخطوات التالية المقترحة

1. **ربط قاعدة البيانات** — استبدال mockData.js باستعلامات Supabase فعلية (الجداول مصمّمة: referees، fitness_tests، registration_requests، activities)
2. **بناء الصفحات الناقصة** — إحصائيات، قائمة الحكام (Tabs/Tiles)، Kanban، List، الرسائل، Inbox، التقويم
3. **تفعيل البحث** — ربط نافذة البحث ببيانات الحكام والاختبارات
4. **ربط الإشعارات** — عرض طلبات التسجيل الجديدة فعلياً
5. **صفحة تعديل الحكم** — نموذج لتعديل بيانات الحكم
6. **صفحة تواصل الإدارة** — نموذج تواصل أو بريد إدارة
7. **تسجيل خروج تلقائي** — انتهاء الجلسة بعد فترة عدم نشاط
8. **تقسيم الكود (Code Splitting)** — الباندل الحالي 771KB، يمكن تحسينه بـ lazy loading للصفحات
